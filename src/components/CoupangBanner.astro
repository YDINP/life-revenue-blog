---
interface CoupangLink {
  title: string;
  url: string;
  imageUrl?: string;
}

interface Props {
  links: CoupangLink[];
}

const DEFAULT_URL = 'https://link.coupang.com/a/dJhOT3';
const { links } = Astro.props;
---

<aside class="coupang-banner">
  <div class="coupang-header">
    <span class="coupang-icon">&#128722;</span>
    <h4>이 글과 함께 보면 좋은 상품</h4>
  </div>
  <div class="coupang-links">
    {
      links.map((link) => (
        <a
          href={link.url || DEFAULT_URL}
          target="_blank"
          rel="noopener noreferrer nofollow"
          class="coupang-link-item"
          data-product={link.title}
        >
          {link.imageUrl && (
            <img src={link.imageUrl} alt={link.title} loading="lazy" />
          )}
          <span class="coupang-title">{link.title}</span>
          <span class="coupang-cta">최저가 확인하기</span>
        </a>
      ))
    }
  </div>
  <p class="coupang-disclaimer">
    이 포스팅은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다.
  </p>
</aside>

<script>
  document.querySelectorAll('.coupang-link-item').forEach(link => {
    link.addEventListener('click', () => {
      const product = link.getAttribute('data-product') || '';
      const url = link.getAttribute('href') || '';
      const slug = window.location.pathname.replace(/^\/blog\//, '').replace(/\/$/, '');
      fetch('https://xyprbsmagtlzebxyxsvj.supabase.co/functions/v1/analytics-ingest', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          event_type: 'coupang_click',
          source: 'blog',
          metadata: { product, url, slug, path: window.location.pathname }
        })
      }).catch(() => {});
    });
  });
</script>

<style>
  .coupang-banner {
    margin: 2.5rem 0;
    padding: 1.5rem;
    border: 2px solid transparent;
    border-radius: 16px;
    background:
      linear-gradient(#fff, #fff) padding-box,
      linear-gradient(135deg, #ff6b35 0%, #e44d26 50%, #ff8c42 100%) border-box;
    box-shadow: 0 4px 20px rgba(228, 77, 38, 0.08);
  }
  :global(.dark) .coupang-banner {
    background:
      linear-gradient(var(--color-bg, #1a1a2e), var(--color-bg, #1a1a2e)) padding-box,
      linear-gradient(135deg, #ff6b35 0%, #e44d26 50%, #ff8c42 100%) border-box;
  }
  .coupang-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }
  .coupang-icon {
    font-size: 1.3rem;
  }
  .coupang-banner h4 {
    margin: 0;
    font-size: 1.05rem;
    color: var(--color-text, #333);
    font-weight: 700;
  }
  .coupang-links {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 0.75rem;
  }
  .coupang-link-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    padding: 1.25rem 1rem;
    border: 1px solid var(--color-border, #e5e7eb);
    border-radius: 12px;
    background: var(--color-bg, #fff);
    text-decoration: none;
    color: var(--color-text, #333);
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }
  .coupang-link-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #ff6b35, #e44d26);
    opacity: 0;
    transition: opacity 0.25s;
  }
  .coupang-link-item:hover {
    border-color: #e44d26;
    box-shadow: 0 8px 24px rgba(228, 77, 38, 0.18);
    transform: translateY(-3px);
  }
  .coupang-link-item:hover::before {
    opacity: 1;
  }
  .coupang-title {
    font-weight: 600;
    font-size: 0.95rem;
    text-align: center;
    line-height: 1.4;
  }
  .coupang-cta {
    display: inline-block;
    padding: 0.5rem 1.25rem;
    background: linear-gradient(135deg, #e44d26 0%, #ff6b35 100%);
    color: #fff;
    font-size: 0.85rem;
    font-weight: 600;
    border-radius: 8px;
    transition: all 0.2s;
    text-align: center;
    width: 100%;
    box-sizing: border-box;
  }
  .coupang-link-item:hover .coupang-cta {
    background: linear-gradient(135deg, #d4431f 0%, #e44d26 100%);
    box-shadow: 0 2px 8px rgba(228, 77, 38, 0.3);
  }
  .coupang-link-item img {
    width: 80px;
    height: 80px;
    object-fit: contain;
    border-radius: 8px;
  }
  .coupang-disclaimer {
    margin: 1rem 0 0;
    font-size: 0.7rem;
    color: var(--color-text-muted, #999);
    text-align: center;
  }
  @media (max-width: 480px) {
    .coupang-links {
      grid-template-columns: 1fr;
    }
  }
</style>

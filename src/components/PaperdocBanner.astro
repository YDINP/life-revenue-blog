---
// 페이퍼닥 프로모션. 모바일/태블릿 = 상단 가로 배너, 웹(데스크톱) = 우측 세로 사이드 배너.
// ⚠️ 파일명/클래스에 "banner"·"ad" 금지: 광고 차단기가 /banner.* 를 네트워크 차단해
//    이미지가 안 뜨고 링크 alt 텍스트만 노출됨. → pd-promo 네이밍 사용.
---
<!-- 상단 가로 배너 (웹에서는 숨김, 모바일/태블릿 노출) -->
<a class="pd-promo-link" href="https://paperdoc-web.vercel.app/" target="_blank" rel="noopener sponsored" aria-label="페이퍼닥 - 계약서 AI 분석 플랫폼 바로가기">
  <span class="pd-promo-frame">
    <picture>
      <source media="(max-width: 767px)" srcset="/pd-promo-m.png" width="1916" height="679" />
      <img class="pd-promo-img" src="/pd-promo.png" alt="페이퍼닥 - 계약서 AI 분석 플랫폼" width="2172" height="724" loading="eager" decoding="async" />
    </picture>
    <span class="pd-promo-shine" aria-hidden="true"></span>
    <span class="pd-promo-cta">무료로 검토받기 <span class="pd-promo-arrow" aria-hidden="true">→</span></span>
  </span>
</a>

<!-- 우측 세로 사이드 배너 (웹 전용, 모바일/태블릿에서는 숨김) -->
<a class="pd-promo-side" href="https://paperdoc-web.vercel.app/" target="_blank" rel="noopener sponsored" aria-label="페이퍼닥 - 계약서 AI 분석 플랫폼 바로가기">
  <img src="/pd-promo-v.webp" alt="페이퍼닥 - 계약서 AI 분석 플랫폼" width="440" height="1320" loading="lazy" decoding="async" />
  <span class="pd-promo-side-cta">무료로 검토받기 <span class="pd-promo-arrow" aria-hidden="true">→</span></span>
</a>

<style>
  .pd-promo-link {
    display: block;
    width: auto;
    margin: -0.5rem -0.5rem 1.5rem;
  }

  .pd-promo-frame {
    position: relative;
    display: block;
    /* CTA를 배너 너비에 비례시키기 위한 컨테이너 (cqw 기준) */
    container-type: inline-size;
    border-radius: 16px;
    overflow: hidden;
    /* 숨쉬기 글로우 — 시선 유도 */
    animation: pd-promo-breathe 3.2s ease-in-out infinite;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .pd-promo-img {
    display: block;
    width: 100%;
    height: auto;
    border-radius: 16px;
  }

  /* 주기적 광택 스윕 */
  .pd-promo-shine {
    position: absolute;
    top: 0;
    left: -60%;
    width: 45%;
    height: 100%;
    pointer-events: none;
    background: linear-gradient(
      100deg,
      transparent 0%,
      rgba(255, 255, 255, 0.05) 35%,
      rgba(255, 255, 255, 0.45) 50%,
      rgba(255, 255, 255, 0.05) 65%,
      transparent 100%
    );
    transform: skewX(-18deg);
    animation: pd-promo-shine 4.5s ease-in-out infinite;
  }

  /* 오버레이 CTA — 하단 URL 우측 빈 영역 */
  .pd-promo-cta {
    position: absolute;
    left: 26%;
    bottom: 9.5%;
    z-index: 3;
    display: inline-flex;
    align-items: center;
    gap: 0.4em;
    padding: 0.52em 1.05em;
    border-radius: 999px;
    /* 배너 너비에 비례(1.35% of banner width) → 배너 축소 시 동일 비율로 축소 */
    font-size: 1.35cqw;
    font-weight: 800;
    color: #0b1020;
    background: #ffd400;
    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.35);
    white-space: nowrap;
    animation: pd-promo-cta-pulse 1.8s ease-in-out infinite;
  }
  .pd-promo-arrow {
    display: inline-block;
    animation: pd-promo-arrow 1.1s ease-in-out infinite;
  }

  /* 호버: 살짝 떠오르고 연출 일시정지, CTA 강조 */
  .pd-promo-link:hover .pd-promo-frame {
    transform: translateY(-3px);
    box-shadow: 0 14px 34px -10px rgba(109, 92, 240, 0.5);
    animation-play-state: paused;
  }
  .pd-promo-link:hover .pd-promo-cta {
    background: #ffe14d;
    transform: scale(1.05);
  }

  /* ── 세로 사이드 배너 + CTA (기본 숨김, 웹에서만 표시) ── */
  .pd-promo-side,
  .pd-promo-side-cta { display: none; }

  @keyframes pd-promo-cta-pulse {
    0%, 100% { transform: scale(1); }
    50%      { transform: scale(1.04); }
  }
  @keyframes pd-promo-arrow {
    0%, 100% { transform: translateX(0); }
    50%      { transform: translateX(4px); }
  }

  @keyframes pd-promo-breathe {
    0%, 100% { box-shadow: 0 0 0 rgba(109, 92, 240, 0); }
    50%      { box-shadow: 0 8px 30px -6px rgba(109, 92, 240, 0.4); }
  }

  @keyframes pd-promo-shine {
    0%   { left: -60%; }
    45%  { left: 120%; }
    100% { left: 120%; }
  }

  /* 모바일(≤767px): 전용 배너 이미지(pd-promo-m)는 레이아웃이 달라
     CTA 위치를 하단 URL 오른쪽 빈 공간으로 재배치(+가독 위해 vw 기준 크기) */
  @media (max-width: 767px) {
    .pd-promo-cta {
      left: auto;
      right: 4%;
      bottom: 10%;
      /* 모바일 배너 너비에 비례(3% of banner width) → 배너 크기에 동적 비례 */
      font-size: 3cqw;
      padding: 0.45em 0.95em;
    }
  }

  /* 웹(넓은 데스크톱, ≥1600px): 상단 가로 배너 숨기고 우측 세로 사이드 배너 노출.
     콘텐츠 최대폭(1240px) 우측에서 정확히 40px 간격을 두고 배치(메인·포스트 공통).
     left = 화면중앙(50%) + 콘텐츠 절반(620px) + 40px gap = 50% + 660px.
     폭은 남은 우측 공간에 맞춰 가변(최소 120·최대 170px). */
  @media (min-width: 1600px) {
    .pd-promo-link { display: none; }
    .pd-promo-side {
      display: block;
      position: fixed;
      left: calc(50% + 660px);
      top: 50%;
      transform: translateY(-50%);
      width: clamp(120px, calc(50vw - 668px), 170px);
      z-index: 40;
      text-decoration: none;
      transition: transform 0.2s ease;
    }
    .pd-promo-side img {
      display: block;
      width: 100%;
      height: auto;
      border-radius: 14px;
      box-shadow: 0 12px 34px -10px rgba(0, 0, 0, 0.4);
      transition: box-shadow 0.2s ease;
    }
    .pd-promo-side:hover { transform: translateY(-50%) scale(1.03); }
    .pd-promo-side:hover img { box-shadow: 0 18px 44px -10px rgba(109, 92, 240, 0.55); }
    /* 이미지 아래 별도 CTA 버튼 */
    .pd-promo-side-cta {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.3em;
      margin-top: 8px;
      padding: 0.62em 0.4em;
      border-radius: 10px;
      font-size: 0.72rem;
      font-weight: 800;
      text-decoration: none;
      color: #0b1020;
      background: #ffd400;
      box-shadow: 0 4px 14px rgba(0, 0, 0, 0.28);
      white-space: nowrap;
      animation: pd-promo-cta-pulse 1.8s ease-in-out infinite;
    }
    .pd-promo-side:hover .pd-promo-side-cta { background: #ffe14d; }
  }

  @media (prefers-reduced-motion: reduce) {
    .pd-promo-frame,
    .pd-promo-shine,
    .pd-promo-cta,
    .pd-promo-side-cta,
    .pd-promo-arrow {
      animation: none;
    }
  }
</style>
